# Optional deps for scripts/test_bookingkai_<strategy>.py — NOT part of the
# main app (requirements.txt). Each strategy script fails fast with an
# install hint if its library is missing, so it's safe to only install a
# subset. Install what you want to try, e.g.:
#
#   .venv/bin/pip install -r scripts/requirements-strategies.txt
#
# Some also need a one-time browser-binary fetch after pip install — see the
# docstring at the top of each script.

playwright>=1.45          # test_bookingkai_playwright_{chromium,firefox,webkit}.py
patchright>=1.45          # test_bookingkai_patchright.py
camoufox[geoip]>=0.4      # test_bookingkai_camoufox.py
seleniumbase>=4.30        # test_bookingkai_seleniumbase_uc.py
DrissionPage>=4.1         # test_bookingkai_drissionpage.py
opencv-python-headless    # test_bookingkai_nodriver_verify_cf.py

# already in the main requirements.txt, listed here for completeness:
# curl_cffi   -> test_bookingkai_curl_cffi.py
# nodriver    -> test_bookingkai_nodriver_stealth.py, test_bookingkai_nodriver_verify_cf.py
